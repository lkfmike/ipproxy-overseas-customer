<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ipproxy.overseas.customer.mapper.StaticProxyPriceMapper">
    <resultMap id="StaticProxyPriceResultMap" type="com.ipproxy.overseas.customer.entity.StaticProxyPrice">
        <id column="id" property="id"/>
        <result column="uid" property="uid"/>
        <result column="type" property="type"/>
        <result column="quality" property="quality"/>
        <result column="area" property="area"/>
        <result column="region" property="region"/>
        <result column="price" property="price"/>
        <result column="discount_price" property="discountPrice"/>
    </resultMap>

    <select id="selectByUidAndType" resultMap="StaticProxyPriceResultMap">
        SELECT id, uid, type, quality, area, region, price, discount_price
        FROM static_proxy_price
        WHERE uid = #{uid} AND type = #{type}
        LIMIT 1
    </select>

    <select id="getDefaultPrice" resultType="java.math.BigDecimal">
        SELECT price
        FROM static_proxy_price
        WHERE uid = 0 AND type = #{type}
        LIMIT 1
    </select>
    
    <select id="selectDefaultByType" resultMap="StaticProxyPriceResultMap">
        SELECT id, uid, type, quality, area, region, price, discount_price
        FROM static_proxy_price
        WHERE uid = 0 AND type = #{type}
        LIMIT 1
    </select>
    
    <select id="selectByUid" resultMap="StaticProxyPriceResultMap">
        SELECT id, uid, type, quality, area, region, price, discount_price
        FROM static_proxy_price
        WHERE uid = #{uid}
    </select>
    
    <select id="selectAllDefaultPrices" resultMap="StaticProxyPriceResultMap">
        SELECT id, uid, type, quality, area, region, price, discount_price
        FROM static_proxy_price
        WHERE uid = 0
    </select>
</mapper>